{
  "name": "tauri-python-sidecar",
  "version": "0.1.0",
  "private": true,
  "description": "Template for shipping a supervised Python sidecar inside a Tauri v2 app on Windows",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --filter ui dev",
    "build": "pnpm --filter ui build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "packageManager": "pnpm@10.20.0"
}
